const styles = (theme) => ({
    root: {
        backgroundColor: '#fff',
        width: '100%',
        display: 'flex',
        justifyContent: 'center',
        padding: 24,
        borderRadius: 8
    }
});

export default styles;